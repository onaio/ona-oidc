[tox]
envlist =
    py{310,311,312}-django{32,42,50}
    lint

[testenv:lint]
deps =
    pipenv
    flake8
    black
basepython = python3.10
commands =
    pipenv sync --dev
    flake8 {toxinidir}/oidc
    black -v {toxinidir}/oidc --check -t py310 -t py311
    isort -c -v {toxinidir}/oidc

[testenv]
deps =
    pipenv
basepython =
    py310: python3.10
    py311: python3.11
    py312: python3.12
commands =
    django32: pip install Django>=3.2.13,<4
    django42: pip install Django>=4.2,<5
    django50: pip install Django>=5.0,<6
    pipenv sync --dev
    python manage.py test {toxinidir}/tests
